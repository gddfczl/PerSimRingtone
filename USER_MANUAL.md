# Per-SIM Ringtone 用户使用说明书

**双卡手机每张 SIM 卡独立来电铃声 · LSPosed 模块**

适用于已解锁 BL、安装 Magisk + LSPosed 的 Android 10~16 设备（实测：摩托罗拉 pstar / LineageOS 23.2）。

## ✅ 实测环境

| 项目 | 配置 |
|---|---|
| 机型 | Motorola pstar (edge 30 Pro / X30) |
| 系统 | LineageOS 23.2 |
| Root | Magisk + Zygisk + LSPosed |

---

## 一、安装

1. 将 `PerSimRingtone-x.x-debug.apk` 传到手机并安装（允许未知来源）。
2. 打开 App，**授予"电话"权限**（读取 SIM 卡信息必需）。

## 二、在 LSPosed 中启用

1. 打开 LSPosed 管理器 → **模块** → 找到 **Per-SIM Ringtone**；
2. 开启模块开关；
3. 点进模块 → 勾选作用域：
   - ✅ **系统（android）**
   - ✅ 系统电信服务（com.android.server.telecom）
4. **重启手机**（必须，telecom 是系统服务）。

## 三、设置铃声

1. 打开 Per-SIM Ringtone App；
2. 每张 SIM 卡会显示：卡槽号、运营商名称、号码掩码、subId；
3. 点击对应卡的 **"选择铃声"** → 在系统铃声选择器中挑选；
4. 界面显示 `当前：已设置自定义铃声 (xxx KB)` 即成功。

### 每张卡的三种状态

| 操作 | 效果 |
|---|---|
| 选择铃声 | 该卡来电播放所选音乐 |
| 铃声选择器中选"无" | 跟随系统默认 |
| "恢复默认"按钮 | 清除该卡配置，跟随系统 |

> 注：本版本"选无"与"恢复默认"效果相同。如需某张卡完全静音，可在系统声音设置里配合勿扰规则实现。

## ⚠️ 重要前提

**用作铃声的音乐必须先添加到系统铃声库**——即先在 系统设置 → 声音 → 来电铃声（标准铃声选择器）里能看到并选中这首歌。本模块的铃声选择器复用系统的铃声库，未入库的音乐不会出现。添加方法：用文件管理器将音频文件设为铃声，或放入手机的 `Ringtones` 目录后重启。

## 四、日常使用须知（重要）

1. **每次开机后打开一次本 App** —— 让进程常驻。否则第一次来电可能先响一声系统默认短音（进程冷启动延迟），之后恢复正常。
2. 如装有 Thanox / 冰箱等冻结类工具，请将本 App 加入**不冻结/自启动白名单**，否则来电时可能读不到铃声配置。
3. 修改铃声后**无需重启**，下次来电即生效。
4. 卸载本 App = 完全还原系统行为，无任何残留。

## 五、故障排查

用电脑连接手机：

```cmd
adb logcat -d | findstr PerSimRingtone
```

| 日志现象 | 含义 |
|---|---|
| 无任何输出 | LSPosed 未启用模块或作用域未勾选 → 检查后重启 |
| `hooked getRingtone(...)` + `hooked RingerAttributes$Builder.build` | 正常运行 |
| 来电时有 `returning Pair(...)` 但响系统铃声 | 铃声文件问题，重新选择一次 |
| `read prefs failed` | App 进程未运行，先打开一次 App |

## 六、工作原理（简述）

来电时系统电信服务通过 `RingtoneFactory.getRingtone()` 决定放什么铃声——这正是"系统只有一个铃声设置"的原因。本模块拦截该调用，根据来电所属 SIM 卡的 subId 返回你在 App 里为这张卡指定的铃声；同时阻止部分 ROM 把响铃交给拨号器 App 处理（否则替换无效）。铃声文件在你选择时已复制到应用私有空间，由本模块安全地提供给系统播放。

## 七、隐私与安全

- 不联网、无任何数据上传；
- 铃声文件仅存于应用私有目录；
- Hook 出错时自动回退系统默认铃声，不会导致无声或崩溃。
